export default function GroupLoading() {
  return (
    <main className="page-shell">
      <section className="page-header">
        <div>
          <div className="eyebrow">Электронный журнал</div>
          <h1 className="page-title">Открываем группу…</h1>
          <p className="page-subtitle">Первое открытие может занять немного времени, дальше журнал будет открываться быстрее.</p>
        </div>
      </section>
      <section className="sheet-card">
        <div className="sheet-card__title">Загружаем Excel и готовим таблицы</div>
      </section>
    </main>
  );
}
